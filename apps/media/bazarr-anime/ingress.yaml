apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bazarr-anime-dashboard-https
  namespace: media
  labels:
    app.kubernetes.io/component: ingress
    app.kubernetes.io/name: bazarr-anime
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`bazarr-anime.powell.place`)
      middlewares:
        - name: security
          namespace: kube-system
      services:
        - name: bazarr-anime
          port: http
