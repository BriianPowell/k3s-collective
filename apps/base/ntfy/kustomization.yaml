apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
commonLabels:
  app.kubernetes.io/part-of: ntfy

resources:
  - deployment.yaml
  - ingress.yaml

configMapGenerator:
  - name: ntfy
    files:
      - server.yaml
