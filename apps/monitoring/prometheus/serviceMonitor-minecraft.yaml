apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minecraft
  namespace: monitoring
  labels:
    monitored-by: prometheus
spec:
  selector:
    matchLabels:
      monitor: minecraft
  namespaceSelector:
    matchNames:
      - minecraft
  endpoints:
    - interval: 30s
      port: minecraft
