apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minecraft
  namespace: monitoring
  labels:
    app.kubernetes.io/component: monitoring
spec:
  selector:
    matchLabels:
      monitor: minecraft
  namespaceSelector:
    matchNames:
      - minecraft
  endpoints:
    - interval: 30s
      port: minecraft
