apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
commonLabels:
  app: prometheus

resources:
  - namespace.yaml
  - config.yaml
  - cluster-role.yaml
  - deployment.yaml
  - ingress.yaml
