apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pihole
  namespace: monitoring
  labels:
    monitoring: prometheus-operator
spec:
  selector:
    matchLabels:
      monitor: pihole
  namespaceSelector:
    matchNames:
      - kube-system
  endpoints:
    - interval: 30s
      targetPort: 9617
