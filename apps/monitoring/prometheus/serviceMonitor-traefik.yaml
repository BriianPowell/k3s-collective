apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: traefik
  namespace: monitoring
  labels:
    monitoring: prometheus-operator
spec:
  selector:
    matchLabels:
      monitor: traefik
  namespaceSelector:
    matchNames:
      - kube-system
  endpoints:
    - interval: 30s
      targetPort: 9100
