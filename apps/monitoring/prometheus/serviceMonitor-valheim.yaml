apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: valheim
  namespace: monitoring
  labels:
    monitored-by: prometheus
spec:
  selector:
    matchLabels:
      monitor: valheim
  namespaceSelector:
    matchNames:
      - valheim
  endpoints:
    - interval: 30s
      port: valheim-1
