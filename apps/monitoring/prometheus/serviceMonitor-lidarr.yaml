apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lidarr
  namespace: monitoring
  labels:
    app.kubernetes.io/component: service-monitor
    app.kubernetes.io/name: lidarr
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: service
      app.kubernetes.io/name: lidarr-exporterr
  namespaceSelector:
    matchNames:
      - media
  endpoints:
    - interval: 30s
      port: metrics
      path: /metrics
