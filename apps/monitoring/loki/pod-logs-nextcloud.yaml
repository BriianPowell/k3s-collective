apiVersion: monitoring.grafana.com/v1alpha1
kind: PodLogs
metadata:
  name: nextcloud
  namespace: monitoring
  labels:
    loki: logs-enabled
spec:
  selector:
    matchLabels:
      app: nextcloud
      component: app
  namespaceSelector:
    matchNames:
      - nextcloud
